# Engineering Decision Register

**Read this first.** Every architectural/implementation discussion and its current state.
Protocol in [README.md](README.md). Don't re-open a **Decided** topic without superseding it.

| ID | Topic | Status | Affects | Owner |
|----|-------|--------|---------|-------|
| [ENG-0001](discussions/0001-hyperv-migration-transport.md) | Hyper-V migration transport for the 2.3 MVP (winrun.py vs Windows agent vs both) | **Decided** (B — agent first, .NET) · **amended 2026-06-16** (agent scoped to HV source, de-front-loaded off 2.0 critical path — via ENG-0009) | ROADMAP 2.0/2.2/2.3, agent-protocol spec, migrate-vm skill | human |
| [ENG-0002](discussions/0002-secret-store.md) | Secret handling: store design & root-key model | **Decided** (ISecretStore + app-native envelope enc, runtime-injected KEK, tokens-over-passwords) · **BUILT 2026-06-18** (re-baselined slice (3)): `ISecretStore`, `AesGcmSecretStore`, `EphemeralSecretStore`, `DekProvider`, `SecretEntity`/`DekEntity`, `AddSecrets` migration | persistence-and-security spec, ARCHITECTURE decision #2, agent mTLS | human |
| [ENG-0003](discussions/0003-agent-install-model.md) | Agent install / onboarding model (remote push vs manual) | **Decided** (manual/org-managed only + enrollment token; no remote install) · **amended 2026-06-16** (HV-only; Proxmox = token+SSH, no install — via ENG-0009) | agent-protocol spec, ROADMAP 2.0/2.2 | human |
| [ENG-0004](discussions/0004-agent-runtime-lifecycle.md) | Agent runtime, packaging, self-update & security model (lightweight/updatable/secure/robust) | **Decided** (NativeAOT, gRPC+mTLS, constrained-verb, gMSA+local, mTLS self-update) · **amended 2026-06-16** (Windows-HV only; descoped from 2.0 front-loading — via ENG-0009) | agent-protocol spec, ROADMAP 2.0/2.2 | human |
| [ENG-0005](discussions/0005-mtls-pki-ownership.md) | mTLS PKI ownership (agent enrollment & trust) | **Decided** (private CA in Core + external seam; root+intermediate; short-lived certs + auto-renew) · **amended 2026-06-16** (CA serves HV fleet only; deferred from 2.0 to migration slice; distinct from ENG-0010 dashboard TLS — via ENG-0009) | agent-protocol spec, persistence-and-security spec, ROADMAP 2.0 | human |
| [ENG-0006](discussions/0006-product-scope-four-pillars.md) | Product scope: four-pillar platform (Observe/Migrate/Deploy/Backup) & sequencing | **Decided** (4 pillars on shared foundation; Observe→Migrate→Deploy→Backup; bidirectional migration HV→PVE first; backup buy-vs-build deferred) | ARCHITECTURE, ROADMAP, all specs, design | human |
| [ENG-0007](discussions/0007-product-strategy-release-scope.md) | Product strategy & release-1 scope: Proxmox-first, HV-as-managed-source, incremental shipping | **Decided** (Proxmox-first super-tool; incremental per-milestone releases, v2.0=planted Observe; HV=light management not parity → provider capability model must allow HV management verbs; weighting Observe→Proxmox mgmt/Deploy→HV→PVE migration; PVE→HV reverse + Backup out of release 1; refines/sequences ENG-0006) · **amended 2026-08-03** (monitoring-first re-focus: **Observe is the product, not the plant** — monitoring reaches *parity across both platforms* while HV management stays light and HV still declines; weighting becomes **Observe (both platforms, to depth) → Proxmox management/Deploy → HV→PVE migration**; Proxmox write verbs demoted behind the monitoring slices — via ENG-0013) | ROADMAP, ARCHITECTURE, PROGRESS, design, ENG-0006 | human |
| [ENG-0008](discussions/0008-rbac-scoped-console-auth.md) | RBAC / scoped roles for the web console (e.g. backup-operator vs vm-operator vs admin) | **Decided** (2026-06-16: fixed roles over pillar×verb catalog reusing ProviderCapability + small console-permission set; local accounts now + OIDC seam later; single audited authz chokepoint before any write verb; firm + early — login in ENG-0010 slice, roles before first write verbs) · **BUILT 2026-06-18** (re-baselined slice (2)): cookie-based session auth, `PasswordHasher`, `IUserStore`/`EfUserStore`, `RbacCatalog`, authz chokepoint middleware, `AddAuth` migration, first-admin seed | ARCHITECTURE §Security, ROADMAP (auth sequencing), persistence schema, all write verbs | human |
| [ENG-0009](discussions/0009-proxmox-deep-action-transport.md) | Proxmox deep-action transport (REST API vs on-node agent vs hybrid) | **Decided** (2026-06-16: A — native PVE REST API primary + constrained SSH key for disk-import/conversion/guest-edit residue; NO agent on Proxmox nodes) | ARCHITECTURE Topology/#1, ROADMAP slice order, proxmox-integration spec, agent foundation scope (0001/0003/0004/0005) | human |
| [ENG-0010](discussions/0010-core-deployment-runtime-model.md) | Core deployment & runtime model (container, 0.0.0.0 bind, TLS, volumes, KEK injection, "install nothing") | **Decided** (2026-06-16: containerized Core, configurable 0.0.0.0 bind, **Core-terminated HTTPS** via Kestrel with self-signed/provided cert + documented reverse-proxy seam, runtime KEK/TLS injection, SQLite on mounted volume, session-token→login+RBAC) · **BUILT 2026-06-17/18** (re-baselined slices (1)–(3)): `Program.cs` hosted-service bootstrap, `TlsSetup.cs`, `Dockerfile`, login+RBAC, `ISecretStore`; **DEPLOYED** at https://vmentorydev.edgestudios.co.za | Program.cs bootstrap, ARCHITECTURE §5/Topology, ROADMAP 2.0, persistence-and-security spec | human |
| [ENG-0011](discussions/0011-observability-logging.md) | Observability / structured logging / failure-surfacing (replace bespoke `ErrorLogger`/`DevLog`; stdout-vs-DB sinks; capability-aware failure messages; audit-vs-ops-log; per-transport diagnostics) | **Open / Raised** (2026-06-17 — trigger: containerized Core on Coolify reports every HV host "unreachable" with no *why*. Broader contract still Discuss + plan, **not decided**) — but one sub-fork resolved: see **ENG-0011a** | Program.cs (`ErrorLogger`/`DevLog`/failure paths), EventHub, persistence-and-security §6/§7, migration-job-model §6, ROADMAP slice order | human |
| [ENG-0011a](discussions/0011-observability-logging.md#decision--eng-0011a-ui-facing-failure-classification--health-tiers) | **(Sub-decision of ENG-0011)** UI-facing failure classification & health tiers — the dashboard-status model | **Decided** (2026-06-19) — **6 tiers** `Healthy/Degraded/Unauthorized/Unreachable/Unconfigured/Unknown` with worst-wins fold `Unreachable>Unauthorized>Degraded>Unconfigured>Healthy` (Unknown=connecting); per-stage CODE taxonomy (`DNS_UNRESOLVED`/`MGMT_PORT_CLOSED`/`MGMT_TRANSPORT_ERROR`/`AUTH_REJECTED`/`AUTH_INSUFFICIENT_PRIV`/`NO_CREDENTIALS`/`SCAN_FAILED`); ICMP informational-only (never folds into tier); **PVE 401→Unauthorized / 403→Degraded split server-side** (ProxmoxProvider.cs:45,92); nullable high-confidence `hint`; **Option A full-depth** — typed `HealthTier`/`FailureStage`/`HostFault`/`HostHealth` in `VMentory.Core`, `IVirtualizationProvider` returns a per-stage fault **list**, one evaluator unifies add-host+poller, `health` JSON over `/api/state`+SSE replaces `Reachability` booleans + `AddError`/`ScanError`/`ErrorDetail`. Unblocks dashboard rebuild. Broader ENG-0011 (substrate/sinks/retention/audit/per-transport-envelope) stays Open. | `VMentory.Core` (new health types), `IVirtualizationProvider`, `ProxmoxProvider`, `HyperVProvider`/`Reachability`, Poller + add-host evaluator, `/api/state`+`EventHub`, `wwwroot/index.html` (1413–1528), Models `Reachability` | human |
| [ENG-0012](discussions/0012-credential-management-ux.md) | Credential management revamp — named/reusable credential entities in `ISecretStore` + a dedicated management UI; structured PVE-token entry (kill the connection-string confusion) | **Decided** (2026-06-19) — **flat `CredentialEntity` (TPH + JSON `Descriptor` + closed C# descriptor union; secrets stay in vault) + typed `Host` slots (`ManagementCredentialId`/`TransportCredentialId`)**. Global creds **retired** (C1) → migrated to a named cred; split storage (A2); delete restricted while referenced w/ 409+usedByHosts (B1); credential owns a **set** of `cred:{Id}:{slot}` vault keys; C1 promotion runs as a **startup task after KEK/DekProvider** (decrypt-to-recompose, Proxmox token decomposed/recomposed bare at the seam — gotcha #13); **Admin-only vault** (`ManageCredentials` Admin-only + new `ViewCredentials` for VmOperator pick-only); CRUD returns **metadata only**, rotation write-only. Slice-5 SSH user (root vs per-node) left as open build-time sub-Q. | `CredentialEntity`+EF/startup-migration, `Host` slots, `ISecretStore` key convention, `RbacCatalog`/`ConsolePermission`, add-host/PATCH + `/api/credentials/*`, credential UI, ENG-0002/0008/0009 | human |
| [ENG-0013](discussions/0013-hyperv-monitoring-transport.md) | Hyper-V monitoring transport from the Linux Core (SSH+PowerShell vs pulling the agent forward vs native WinRM) | **Decided** (2026-08-03) — **A: SSH + PowerShell executed on the host**, key auth, no Windows password at rest. Trigger: HV is **wholly unreachable** from the container — `Reachability.cs:169` shells `powershell.exe`, `:20` shells `ping.exe`, `:52,80-82` ensure the *local* WinRM service + `TrustedHosts`, and `Scanner.cs:216` assumes Core is the domain WinRM **client**; only `Program.cs:517`'s TCP probe survives on Linux (this is the ENG-0011 trigger's root cause). One **`ISshExecutor`** seam in `VMentory.Core` serves **both** platforms (HV commands + ENG-0009 PVE residue), built once **for monitoring** rather than arriving with the Proxmox write-verb slice. `BuildRemoteWrapper`/`Invoke-Command`, `RunPowerShellAsync`, the WinRM ensure and `TrustedHosts` are **deleted, not ported**; ICMP becomes genuinely optional (non-root container, no raw sockets — consistent with ENG-0011a informational-only). HV gains an SSH key as a 2nd per-host secret → **ENG-0012 must land first**. Agent + private CA (ENG-0001/0003/0004/0005) **off the monitoring path entirely**, narrowed to the migration source. Open build-time sub-Qs: plain `ssh` exec vs pwsh-7 PSRP (start plain, 5.1 is in-box), SSH user model (resolve with ENG-0012's root-vs-per-node), and a **live-HV verification gate** (nothing proven against a real host yet). | `HyperVProvider`/`Scanner`/`Reachability`/`Poller`, new `ISshExecutor` in Core, Dockerfile, ROADMAP slice order, ARCHITECTURE Topology, ENG-0007 weighting, ENG-0011a fault codes | human |

| [ENG-0014](discussions/0014-sso-oidc-console-auth.md) | SSO / OIDC sign-in for the console (one identity provider across the estate; lands the ENG-0008 "OIDC seam later") | **Decided** (2026-08-25) — generic **authorization-code + PKCE** via `Microsoft.AspNetCore.Authentication.OpenIdConnect` (the solution's only NuGet dep), **issuer is config not a hardcoded provider** so a customer deployment points at the customer's IdP; OIDC **signs into the existing cookie scheme** and the OP principal is **discarded and rebuilt** with the password path's exact claim shape (`RbacCatalog` reads `ClaimTypes.Role` — a different shape fails closed); SSO users are auto-provisioned `AppUserEntity` rows keyed by email with `PasswordHash=""` (unreachable via `/api/auth/login` by construction); **first login takes `VMENTORY_OIDC_ROLE` (default Admin), later logins take the stored role**; SSO accounts never enter forced rotation; `VMENTORY_PASSWORD_LOGIN=0` closes the local door and `=1` is the break-glass. **Cookie drops Strict→Lax only when OIDC is configured** (Strict withholds the cookie on the post-callback redirect; Lax still blocks cross-site write verbs). Group→role mapping **deferred** — the Pocket-ID instance has zero groups. | `Program.cs` auth registration + `/api/auth/*`, `VMentory.Web.csproj`, `wwwroot/index.html` login screen, ENG-0008, ENG-0010 | human |

| [ENG-0015](discussions/0015-estate-dashboard-remediation-tracker.md) | Estate dashboard & remediation tracker — physical-machine view joining the hardware monitor (Dell OpenManage), hypervisor inventory and an interactive action list (tick-off, dependencies, maintenance windows, per-action VM blast radius) | **Decided + BUILT** (2026-08-25) — extend VMentory, not a new app; `Machine` is the join point across three mutually-unaware sources (service tag / address / slug); the hardware monitor is optional env config (`VMENTORY_OME_*`), not a provider; **VMentory is the one source of truth for the action list** (Outline tracker imported once as a seed, item numbers preserved, never re-applied); priority = consequence, class = logistics; collector-raised actions are keyed `ome:{tag}:{fault}` and adopt seeded items with the same key, reopen on recurrence, never auto-close; blast radius computed from live inventory else the labelled static list; deps are cycle-rejected edges; writes need new `ConsolePermission.ManageActions`. Deployed instance is inside the client network (VM 171 on vega14) so OME is read directly. Hyper-V live inventory still gated on ENG-0013. | `VMentory.Core/Estate`, `EstateEntities` + `AddEstate` migration, `OmeClient`/`EstateCollector`/`EstateSeed`/`EstateEndpoints`, `wwwroot/index.html` + `estate-seed.json`, `RbacCatalog`/`ConsolePermission`, ROADMAP slice 8 | human |

<!-- Add new rows at the bottom. Keep IDs sequential. Next free ID: ENG-0016. -->

## Legend
**Open** → raised, not analyzed · **In discussion** → being worked · **Awaiting decision** → needs the human owner · **Decided** → relied upon downstream · **Superseded** → replaced (link the successor)
